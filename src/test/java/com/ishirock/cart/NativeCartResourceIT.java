package com.ishirock.cart;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeCartResourceIT extends CartResourceTest {

    // Execute the same tests but in native mode.
}